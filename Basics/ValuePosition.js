/* class for a sorting function, position in the multidimensional array, containing the indizes for as many dimensions as needed*/
class ValuePosition {
    constructor(value, pos){
        this.value = value;
        this.pos = pos;
    }
}